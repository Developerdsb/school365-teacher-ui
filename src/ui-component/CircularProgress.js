import React from 'react';
import { CircularProgress } from '@mui/material';

const CircleProgress = () => {
  return <CircularProgress size={25} thickness={4.7} />;
};

export default CircleProgress;
